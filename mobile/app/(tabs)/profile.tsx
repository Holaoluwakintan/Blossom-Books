import { StyleSheet, Text, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { colors, spacing } from "@/constants/theme";

export default function ProfileScreen() { return <ScreenContainer className="px-5" edges={["top", "left", "right"]}><View style={styles.content}><Text style={styles.kicker}>YOUR BLOSSOM PROFILE</Text><Text style={styles.title}>Your reading life, in one place.</Text><Text style={styles.body}>Mobile sign-in will use the existing Blossom OAuth session through a secure browser callback. This screen is the foundation for account, library, writer, and deletion flows.</Text><View style={styles.card}><Text style={styles.cardTitle}>Next mobile milestone</Text><Text style={styles.cardText}>Connect OAuth, secure token storage, saved stories, reading progress, and account deletion.</Text></View></View></ScreenContainer>; }
const styles = StyleSheet.create({ content: { paddingTop: spacing.xl }, kicker: { color: colors.primary, fontSize: 11, fontWeight: "700", letterSpacing: 2 }, title: { color: colors.foreground, fontSize: 32, fontWeight: "700", lineHeight: 38, marginTop: 10 }, body: { color: colors.muted, fontSize: 16, lineHeight: 24, marginTop: 16 }, card: { backgroundColor: colors.lavender, borderRadius: 16, marginTop: 26, padding: 20 }, cardTitle: { color: colors.foreground, fontSize: 17, fontWeight: "700" }, cardText: { color: colors.muted, fontSize: 14, lineHeight: 21, marginTop: 8 } });
